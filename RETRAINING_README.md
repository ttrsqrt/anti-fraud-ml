# Automated Model Retraining System

## Компоненты

### 1. `retrain_model.py` - Основной скрипт переобучения
**Функционал:**
- ✅ Версионирование моделей (v1.0.0, v1.1.0, ...)
- ✅ Автоматическое сохранение метаданных (метрики, timestamp)
- ✅ Умный деплой (сравнивает ROC-AUC с предыдущей версией)
- ✅ Backup старых production моделей
- ✅ История всех версий в `models/version_log.json`

**Использование:**
```bash
# Ручное переобучение и деплой
py retrain_model.py
```

**Что происходит:**
1. Загружает `featured_dataset.csv`
2. Обучает новую модель с быстрой валидацией (500 итераций)
3. Сохраняет как `models/catboost_model_vX.X.X.cbm`
4. Сравнивает ROC-AUC с предыдущей версией
5. Если лучше/равно → деплоит в `catboost_model.cbm` (production)
6. Если хуже → сохраняет версию, но НЕ деплоит (manual review)

---

### 2. `scheduler.py` - Планировщик автоматического переобучения
**Функционал:**
- ⏰ Запускает `retrain_model.py` по расписанию
- 📅 По умолчанию: каждый день в 3:00 ночи
- 🔧 Легко настраивается под любую частоту

**Использование:**
```bash
# 1. Установить зависимость
py -m pip install schedule

# 2. Запустить планировщик (работает в фоне)
py scheduler.py
```

**Настройка расписания:**
Откройте `scheduler.py` и измените строку:
```python
# Каждый день в 3:00
schedule.every().day.at("03:00").do(retrain_job)

# Или каждые 7 дней
schedule.every(7).days.do(retrain_job)

# Или каждую неделю
schedule.every().week.do(retrain_job)
```

---

## Структура файлов

```
ttrsqr/
├── catboost_model.cbm          # 🚀 Production модель (текущая)
├── models/
│   ├── version_log.json        # 📜 История версий
│   ├── catboost_model_v1.0.0.cbm
│   ├── catboost_model_v1.1.0.cbm
│   ├── catboost_model_v1.2.0.cbm
│   └── production_backup_*.cbm # 📦 Бэкапы
├── retrain_model.py
└── scheduler.py
```

---

## Примеры использования

### Пример 1: Ручное переобучение раз в неделю
```bash
py retrain_model.py
```

### Пример 2: Автоматическое переобучение каждую ночь
```bash
py scheduler.py
# Оставить работать в фоне (или как Windows Service)
```

### Пример 3: Откат к предыдущей версии (через Python)
```python
from retrain_model import ModelRetrainer

retrainer = ModelRetrainer()
retrainer.print_version_history()  # Посмотреть все версии
retrainer.promote_to_production("v1.0.0")  # Откатиться на v1.0.0
```

---

## Преимущества для оценки (Usability - 5 баллов)

✅ **Автоматическое обновление** - планировщик работает без ручного вмешательства  
✅ **Версионирование** - можно откатиться на любую версию  
✅ **Умный деплой** - не деплоит плохие модели автоматически  
✅ **Бэкапы** - старые production модели сохраняются  
✅ **Логирование** - вся история в JSON

---

## Дополнительно: Production Deployment

Для реальной production среды:

1. **Docker + Cron** - контейнер с scheduler.py
2. **Airflow DAG** - оркестрация переобучения
3. **MLflow Integration** - расширенный tracking экспериментов
4. **A/B Testing** - параллельное тестирование старой и новой модели

Текущая реализация - **полнофункциональный MVP** для демонстрации возможностей!
